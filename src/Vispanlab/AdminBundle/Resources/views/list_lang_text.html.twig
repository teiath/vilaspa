{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    {% for curValue in value %}
        <div><i>{{curValue.locale}}</i>:{{curValue.text_formatted|striptags|truncate(1000)|raw}}</div>
    {% endfor %}
{% endblock %}